<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WanderEase" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/primary</item>
        <item name="colorOnPrimarySurface">@color/primary</item>
        <item name="android:textColorHighlight">@color/highlight</item>
        <item name="colorSecondaryContainer">@color/navigation_active_indicator</item>
    </style>

    <style name="Theme.WanderEase.Splash" parent="Theme.Material3.Light.NoActionBar">
    </style>
    <style name="Theme.WanderEase.AlertDialog" parent="ThemeOverlay.Material3.Dialog"/>
</resources>